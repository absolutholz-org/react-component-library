import { Canvas, Story, ArgsTable } from '@storybook/addon-docs';

# Switch

The switch component is an alternative binary form selection element.

It must be used within a `form` context.

<Canvas>
    <Story id='ui-oxide-23-oxswitch--unchecked' />
    <Story id='ui-oxide-23-oxswitch--checked' />
</Canvas>

## Component API

<ArgsTable />
